services:
  dev:
    build: .
    privileged: true
    stdin_open: true
    tty: true
    volumes:
      - .:/app
      - app_node_modules:/app/node_modules
    working_dir: /app
    command: sleep infinity

volumes:
  app_node_modules:
